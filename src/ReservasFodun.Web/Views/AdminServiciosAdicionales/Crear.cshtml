@model ReservasFodun.Application.DTOs.Admin.ServicioAdicionalFormularioRequest

@{
    ViewData["Title"] = "Crear servicio adicional";
}

<div class="mb-4">
    <h1 class="page-title">Crear servicio adicional</h1>
    <p class="page-subtitle">Registra un nuevo servicio adicional para las reservas.</p>
</div>

<div class="section-card">
    <form asp-action="Crear" method="post">
        @Html.AntiForgeryToken()

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <partial name="_FormularioServicioAdicional" model="Model" />

        <div class="d-flex flex-wrap gap-2 mt-4">
            <button type="submit" class="btn btn-primary">Guardar servicio</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Volver</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
