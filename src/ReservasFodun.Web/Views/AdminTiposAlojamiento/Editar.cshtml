@model ReservasFodun.Application.DTOs.Admin.TipoAlojamientoFormularioRequest

@{
    ViewData["Title"] = "Editar tipo de alojamiento";
}

<div class="mb-4">
    <h1 class="page-title">Editar tipo de alojamiento</h1>
    <p class="page-subtitle">Actualiza la información del tipo seleccionado.</p>
</div>

<div class="section-card">
    <form asp-action="Editar" method="post">
        @Html.AntiForgeryToken()

        <input asp-for="IdTipoAlojamiento" type="hidden" />

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <partial name="_FormularioTipoAlojamiento" model="Model" />

        <div class="d-flex flex-wrap gap-2 mt-4">
            <button type="submit" class="btn btn-primary">Guardar cambios</button>
            <a asp-action="Detalle" asp-route-id="@Model.IdTipoAlojamiento" class="btn btn-outline-primary">Ver detalle</a>
            <a asp-action="Index" class="btn btn-outline-secondary">Volver</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
